version: "3.9"
services:
  web:
    build: .
    ports:
      - "8001:8001"
    # command: python3 server.py
    volumes:
      - .:/app
    container_name: scholarscrib-api
    image: scholarscrib-api:v1
    env_file:
      - .env.prod
